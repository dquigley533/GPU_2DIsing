project('gasp',           # The name of the project
        ['c', 'cuda'],    # The languages that Meson should initialize

        # Keyword arguments:
        version : '0.0.1',
        default_options : ['optimization=0', 'debug=true', 'warning_level=3'],
)
subdir('src')
